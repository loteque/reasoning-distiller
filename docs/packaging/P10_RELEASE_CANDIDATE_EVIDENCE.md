# P10 Release-Candidate Evidence

Status: **BLOCKED — RUNTIME ISOLATION**

Repository: `loteque/reasoning-distiller`

## Bound identities

- coordination control: `main@80b6e89ad2efe84b088ca06b908a257c449fac15`
- closed P10 semantic candidate: `ae5d5c21de1f646b7b7c4450a1f9e8db6fcbcf0e`
- closed P10 tree: `81178c5efdc8f1419a068c61a92c0571b28f69fc`
- P10 Engineer evidence: `ff09627f8e83abc60f430b378bed342cbeaceb79`
- P10 Engineer evidence run: `32933482638`
- P10 independent review: `53928833ff2735c08615c966c91e50f08322b4df`
- P10 independent disposition: `P10_G9_INDEPENDENT_REVIEW_PASS`
- P10 Steward closure: `631906e33c701d8a1eb6257b29d2402b23c9de28`
- P10 Steward disposition: `P10_STEWARD_RECONCILIATION_ACCEPTED`

## Integration and clean-source candidates

The post-P10 integration candidate retaining the live `main` operational baseline is:

- branch: `rc/p10-integration-20260825`
- head: `b7c36e2fc8c9b09d899f4468e660e70a1d31a2c6`
- tree: `bccd930310492375f8982e07dfd7f27370d8cdb0`

The clean release-source candidate produced by removing only `.reasoning-distiller/` from that integrated source is:

- branch: `rc/p10-release-source-20260825`
- head: `5bfd2a00232421ee56547d0b2e577398d10d2d45`
- tree: `a4d419c60a62e5968c93b40baaa1dfe310a51bdf`

The release-preparation integration adds no P10 semantic changes. It retains the exact closed P10 semantic paths and carries live-main operational state plus release-workflow coverage for the new `context_packaging` managed root.

## Evidence execution

Exact evidence workflow commit:

- `cc1f41b58abd493a114cb1ac468b9cba082e5fa0`

Exact GitHub Actions evidence:

- run: `32940284646`
- job: `98089685872`
- runtime: CPython `3.12.0` / `cpython-312`
- conclusion: **FAILURE**

The run established PASS evidence for all gates completed before the blocker:

1. exact clean RC source commit and tree identity;
2. absence of `.reasoning-distiller/` from the clean source;
3. byte identity of the closed P10 semantic paths against `ae5d5c21de1f646b7b7c4450a1f9e8db6fcbcf0e`;
4. clean-source transformation deleting only `.reasoning-distiller/**` from the integrated source;
5. release workflow coverage for the `context_packaging` managed root;
6. package-builder, package-contract, and installer-runner contract suites;
7. two deterministic package builds with byte-identical archive, manifest, and detached digest;
8. generated manifest validation and exact expected package top-level roots;
9. P3 deterministic installer tests;
10. P4 recovery tests;
11. production invocation tests.

The complete context-packaging suite, extraction-parity checks, final evidence report, and evidence artifact upload were not reached because the runtime-isolation gate failed closed.

## Exact RC package identity

The deterministic RC package generated by run `32940284646` has:

- version: `0.0.0-p10-rc1`
- source commit: `5bfd2a00232421ee56547d0b2e577398d10d2d45`
- file count: `98`
- content identity: `sha256:4b9440f1baa1825c4e9f665f68ef71629dbcf4611c1a062a4fe0f8f1ff236c3a`
- transport SHA-256: `6fa51bcbe8642a8b575de5e3f2f1398fc61c5a8a560e7b8bbf7a74b3fc97e373`

## Blocking finding

Disposition for this release-preparation evidence:

`P10_RELEASE_CANDIDATE_BLOCKED_RUNTIME_ISOLATION`

The existing `reasoning-distiller-runtime-isolation-audit/1` rejected the packaged file:

`schemas/context-pack.schema.json`

at:

`$.$defs.knowledgeItem.properties.pems.$ref`

because the exact closed P10 schema contains this source-repository reference:

`https://github.com/loteque/reasoning-distiller/blob/main/backends/pems-cove/pems-v2.schema.json`

The audit reported:

`forbidden-runtime-reference:$.$defs.knowledgeItem.properties.pems.$ref:github.com/loteque/reasoning-distiller`

This is not treated as provenance-only metadata. A JSON-Schema `$ref` is behavior-bearing resolution input, while the accepted release isolation contract forbids executable dependency on the generic repository.

## Authority and scope boundary

No release was published. No tag was created. `main` was not changed. `release-source` was not changed. No admission, canonical-state mutation, role/authority mutation, or activation mutation was performed.

This work unit did not alter the closed P10 schema or weaken the runtime-isolation audit. Either action would cross the supplied post-P10 release-preparation scope because it would change closed P10 semantic bytes or an accepted release/runtime-isolation contract.

Release preparation therefore stops at this blocker. A remediation requires a separately bounded and authorized workflow that first establishes the governed path for changing the behavior-bearing schema/reference or the governing isolation contract. Any new P10 semantic candidate must regain the review and reconciliation evidence required by the live repository contracts before release preparation can resume.
